<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>


    <style name="CrearCuentaEditText" parent="AppTheme">
        <item name="android:padding">8dp</item>
        <item name="android:maxLines">1</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textColorHint">@color/white</item>
        <item name="android:background">@color/edit_text_background</item>
        <item name="android:layout_marginTop">10dp</item>
    </style>

    <style name="DefaultBackground" parent="AppTheme">
        <item name="android:background">@color/colorPrimarySuperLight</item>
    </style>

    <style name="DefaultCardView" parent="AppTheme">
        <item name="cardBackgroundColor">@color/grey_100</item>
        <item name="cardCornerRadius">2dp</item>
        <item name="cardElevation">3dp</item>
        <item name="cardUseCompatPadding">true</item>

    </style>

    <style name="PrimaryText" parent="AppTheme">
        <item name="android:textColor">@color/colorPrimaryText</item>
        <item name="android:textSize">@dimen/l_font_size</item>
    </style>

    <style name="SecondaryText" parent="AppTheme">
        <item name="android:textColor">@color/colorSecondaryText</item>
        <item name="android:textSize">@dimen/s_font_size</item>
    </style>

</resources>
