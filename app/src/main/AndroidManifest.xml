<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ndiaz.parquesbsas">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.MainActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.SplashScreen"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.CrearCuenta"
            android:screenOrientation="portrait" />
        <activity android:name=".activities.MainHome"></activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".activities.MySettings" />
        <activity android:name=".activities.old.ListaParques" />
        <activity
            android:name=".activities.old.AgregarParque"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.old.DetallesParqueOld"
            android:screenOrientation="portrait" />
        <activity android:name=".activities.reclamos.ListaReclamos">
            <!--<intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>-->
        </activity>
        <activity android:name=".activities.info_parques.DetallesParque">
            <!--<intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>-->
        </activity>
        <activity android:name=".activities.info_parques.DescGeneralParque" />
        <activity android:name=".activities.info_parques.ListaActividadesParque" />
        <activity android:name=".activities.info_parques.ListaActividadesHorarios" />
        <activity
            android:name=".activities.reclamos.AgregarReclamo"
            android:screenOrientation="portrait" />
        <activity android:name=".activities.reclamos.DetalleReclamo" />
        <activity android:name=".activities.PerfilUsuario" />
        <activity
            android:name=".activities.reclamos.CamaraReclamo"
            android:screenOrientation="landscape"></activity>
    </application>

</manifest>